--- 
- hosts: all
  become: yes
  tasks:
  - name: install webserver
    apt: 
      name: apache2
      state: present
    when: ansible_os_family=="Debian"

  - name: i want to restart webserver
    service: 
       name: apache2
       state: restarted
    when: ansible_os_family=="Debian"

  
